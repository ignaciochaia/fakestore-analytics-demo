# Copy this file to .env before running the ETL locally.
# Never commit real credentials to version control.
PG_CONN_STR="postgres://postgres:YOUR_PASSWORD@db.YOUR_SUPABASE_PROJECT.supabase.co:5432/postgres?sslmode=require"
# Optional: override FakeStore API base URL (useful when direct access is blocked)
# FAKESTORE_BASE_URL="https://r.jina.ai/https://fakestoreapi.com"
